[project]
name = "python-scripts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jsonpath-ng>=1.7.0",
    "pywinctl>=0.4.1",
    "ruff>=0.9.6",
]
